name: pre-commit

on:
  push:
  pull_request:

jobs:
  pre-commit:

    runs-on: ubuntu-latest

    env:
      ACCESS_SECRET_KEY: test-access-secret-key-min-32-chars-long
      RESET_PASSWORD_SECRET_KEY: test-reset-secret-key-min-32-chars-long
      VERIFICATION_SECRET_KEY: test-verification-secret-key-min-32-chars


    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install uv
        uses: astral-sh/setup-uv@v5

      - name: Install the project
        working-directory: ./backend
        run: uv sync --all-extras --dev

      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9
          run_install: false

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install Node dependencies
        working-directory: ./frontend
        run: pnpm install

      - name: Run pre-commit
        uses: pre-commit/action@v3.0.1